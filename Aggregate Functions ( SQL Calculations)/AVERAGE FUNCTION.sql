-- 'AVG()' Function
-- Calculate the average value of a particular column.
-- It works by taking a column as argument and returns the average for that column.



SELECT AVG(ID) AS AverageID FROM Aggregates

SELECT AVG(DISTINCT ID) AS UniqueIdAverage FROM Aggregates